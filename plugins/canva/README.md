# Canva Plugin Package

This folder is the shared Canva plugin package used by Claude Code, Cursor, and
Codex. It contains the host-specific plugin manifests, the Canva MCP
registration, and the active `skills/` directory.

See the repository root `README.md` for marketplace-level install instructions.
